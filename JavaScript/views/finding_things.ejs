<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <title>
        <%= title %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="/common.js"></script>
</head>

<body class="bg-light">

    <div class="container py-5">
        <div class="mx-auto" style="max-width: 500px;">
            <div class="card shadow-sm">
                <div class="card-body">

                    <h2 class="card-title text-center mb-3">🛒食材や道具を探し中</h2>

                    <% if (message) { %>
                        <div class="alert alert-success">
                            <%= message %>
                        </div>
                        <% } %>

                            <h4 class="mb-3 text-primary text-center">
                                <%= title %>
                            </h4>

                            <div class="mb-4 p-3 bg-body-secondary rounded">
                                <div class="mb-2 fw-bold">以下から最大2つまで選んでください</div>
                                <form method="POST" action="/finding_things">
                                    <select class="form-select mb-3" name="selectedItem[]" multiple size="6">
                                        <% items.forEach(item=> {
                                            const cleanItem = item.replace(/　/g, ' ').trim();
                                            %>
                                            <option value="<%= cleanItem %>">
                                                <%= cleanItem %>
                                            </option>
                                            <% }) %>
                                    </select>
                                    <div class="text-end">
                                        <button type="submit" class="btn btn-warning">✅ 決定</button>
                                    </div>
                                </form>
                            </div>
                            <div class="d-grid gap-2 mt-4">
                                <a href="/" class="btn btn-outline-danger">🏠 HOMEに戻る</a>
                                <a href="/#explore" class="btn btn-outline-secondary">🪵 食材探索ゾーンに戻る</a>
                            </div>

                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>